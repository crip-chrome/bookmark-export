import {Status} from './Status';
import {ChromeBookmarks} from 'chrome.bookmarks';

export class Storage {

    constructor(private status: Status, defaultApiUrl: string) {
    }

    public saveApiUrl():boolean {
        let self = this;
    Chrome.
        
        return false;
    }
}